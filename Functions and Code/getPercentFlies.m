
function n = getPercentFlies(x_loc,Total)
% n = getPercentFlies(x_loc,Total)
% e.g.: n = (sum(x_loc,2)./Total)*100;
    n = (sum(x_loc,2)./Total)*100;

    % a = sum(x_loc,2);
    % b = a./Total;
    % c = b.*100;
    % 
    % figure; hold on
    % plot(a)
    % plot(Total)
    % 
    % figure; hold on
    % plot(b)
    % yyaxis right
    % plot(c)
    % 
    % figure; plot(n)

    
end